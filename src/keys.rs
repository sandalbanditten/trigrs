use crate::model::Model;
use nannou::prelude::{App, Key};

pub fn key_pressed(_app: &App, _model: &mut Model, key: Key) {
    match key {
        _ => (),
    }
}

pub fn key_released(_app: &App, _model: &mut Model, key: Key) {
    match key {
        _ => (),
    }
}
